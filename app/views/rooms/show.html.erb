<h1>Chat</h1>
<ul id="chat-index">
  <% @content.each do |message| %>
  <li><%= message.content %></li>
  <% end %>
</ul>
<%= form_tag(rooms_path, method: :post) do %>
  <input id="chat-form" type="text" data-behavior="chat_post" name="message">
<% end %>
